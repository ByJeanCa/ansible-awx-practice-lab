events {}
http {
   server {
       listen {{ port }};
       server_name {{ servername }};
       root /var/www/html;
       index index.html;
       location / {
           try_files $uri $uri/ =404;
       }
   }
}